[package]
name = "dice"
version = "0.1.0"
edition = "2021"


[lib]
crate-type = ["cdylib"]

[dependencies]
botadapt-plugin-sdk = { path = "../../botadapt-plugin-sdk" }
rand = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
